[build]
  command = "vite build"
  publish = "dist"

# Angel is a client-side SPA. Always serve index.html for application routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
